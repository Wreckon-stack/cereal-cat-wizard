Drop the reference image here as `hero-bg.jpg` to use it as the fullscreen
background (the one with the party-hat cat, Froot Loops, and the bowl).

If no file is present, ParallaxBackground.tsx renders a painted cloud + sun
fallback automatically — no broken layout either way.

Optional files you can drop in:
  hero-bg.jpg   ← fullscreen parallax hero background
